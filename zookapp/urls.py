from django.conf.urls import url

from . import views

# urlpatterns = [
#     url(r'^$', views.index, name='index'),
#     url(r'^(?P<solarsystem_id>[0-9]+)/$', solar_system, name='solar_system'),
#
# ]
